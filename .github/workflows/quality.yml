name: TerraLoop quality
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: JavaScript syntax check
        run: node --check app.js
      - name: Required submission files
        run: |
          test -s index.html
          test -s styles.css
          test -s app.js
          test -s README.md
          test -s DEVPOST.md
          test -s DEMO_SCRIPT.md
